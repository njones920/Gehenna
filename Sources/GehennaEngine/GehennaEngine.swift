// MARK: - GehennaEngine
// A physics engine for ancient cosmology, disguised as a game.

/// GehennaEngine — the core simulation and grammar resolution engine for GEHENNA.
///
/// This package contains:
/// - The ritual grammar DSL (result builder for composing ritual configurations)
/// - The 13-stage resolution pipeline (deterministic ritual → spirit compilation)
/// - The dual-layer fragment model (structured properties + narrative tags)
/// - The cosmological state machine (7 regional variables, Veil computation, entropy decay)
/// - The Astragali diagnostic system (pre-ritual divination/debugging)
/// - The Ritual Autopsy system (post-ritual diegetic feedback)
/// - The Authority Gate (token-based ritual access control)
///
/// Design commitments:
/// - Deterministic: same canon + same pre-state + same intent + same entropy seed = same event
/// - No visible numbers: all feedback is behavioral, not numerical
/// - Entropy asymmetry: destabilization is fast, recovery is slow
/// - The practitioner is the entropy
public enum GehennaEngine {
    public static let version = "0.4.24"
    public static let codexVersion = "3.0"
}
